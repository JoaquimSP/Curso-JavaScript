function soma(n1=0, n2=0){
   // se não forem chamados dois valores, o javascript irá retornar Nan = not a number. ------ igualando a 0 não acontecerá esse erro.
   return n1 + n2
}

// console.log(soma(3, 2)) // passando dois valores
console.log(soma(3)) // passando só um valor